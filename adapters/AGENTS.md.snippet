# 粘贴到目标项目 AGENTS.md（或与其合并）

```markdown
## Gated Agent Workflow

本项目使用门禁式 AI 开发流程（Gated Agent Workflow）。

- 必读：`docs/ai-workflow/PLAYBOOK.md`（若使用 submodule/外部仓库，改为对应路径）
- 模板：`docs/ai-workflow/templates/`
- 新项目 → Bootstrap；维护/新需求 → Change Request（默认先只读勘察）
- 硬约束：可执行验收；一步一步；每步硬停；产出可复制下一 Handoff；禁止范围外重构
```
