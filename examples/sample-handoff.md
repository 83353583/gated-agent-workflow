# 示例 Handoff | Step 1 → Step 2

> 虚构示例，展示密度与结构。

## 你是谁 / 本步目标

- 角色：实现工程师（只做本步）
- 本步目标：在已有 API 之上做出「列出任务 + 新建任务」的最小 UI，打通 MVP 演示路径
- 明确不做：编辑/删除 UI、账号系统、样式体系重构

## 全局约束（只读摘要）

- 技术栈：Node + 既有前端骨架（见 `ARCHITECTURE.md`，已冻结）
- 范围：仅 MVP Must #1–2
- 禁止改动：`src/auth/**`（不存在也勿创建）、不要改 API 契约除非测试同步更新
- 文档：`docs/ai-workflow/SCOPE.md`

## 上一步已完成（事实）

- 完成内容：任务 CRUD API + 测试通过
- 关键文件：`src/api/tasks.ts`、`tests/api/tasks.test.ts`
- 关键决策：内存存储仅用于 MVP
- 已知坑：进程重启丢数据（已记入决策日志，可接受）

## 本步输入

- 优先阅读：`src/api/tasks.ts`、`src/ui/App.tsx`、`SCOPE.md` MVP 验收
- 依赖接口：`GET /tasks`、`POST /tasks`

## 本步交付物

1. 列表展示已有任务
2. 表单可新建并刷新列表

## 验收

```bash
npm test
npm run dev   # 人工：打开页面，新建一条，列表可见
```

- 通过标准：测试绿；人工演示路径可走通
- 失败时停下

## 回归范围

- API 测试仍须全绿

## 禁止改动

- 不要替换状态管理库；不要改端口约定

## 模型档位建议

- 建议：中
- 升级条件：若需引入复杂客户端缓存再升到高

## 结束时你必须输出

1. 变更文件清单
2. 验收结果摘要
3. Step 2 → Step 3 的下一份 Handoff
